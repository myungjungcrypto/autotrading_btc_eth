{
  "name": "cascade-risex-arbi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Delta-neutral arbitrage monitor and execution shell for Cascade and RISEx.",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test test/*.test.js",
    "check": "node --check src/*.js src/**/*.js test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
